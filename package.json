{
  "name": "figma-to-ai-json",
  "version": "1.0.0",
  "description": "Figma plugin that exports designs to AI-optimized JSON format for LLMs",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es6 && cp src/ui.html dist/ui.html",
    "watch": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es6 --watch",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.{ts,tsx,js,jsx,html,css,json}\"",
    "format-and-check": "pnpm run format & pnpm run typecheck"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "esbuild": "0.25.0",
    "prettier": "^3.7.4",
    "typescript": "^5.7.2"
  }
}
